name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Project
        uses: actions/checkout@v4

      - name: Install Rokit
        uses: CompeyDev/setup-rokit@v0.1.2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Build
        run: |
          rojo build default.project.json -o rubine.rbxm

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: rubine.rbxm

  release:
    name: Release
    needs: [build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Project
        uses: actions/checkout@v4

      - name: Download Build
        uses: actions/download-artifact@v4
        with:
          name: build
          path: build

      - name: Move Build
        run: mv build/rubine.rbxm rubine.rbxm

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: Rubine ${{ github.ref_name }}
          files: |
            rubine.rbxm

  publish:
    name: Publish
    needs: [release]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Project
        uses: actions/checkout@v4

      - name: Install Rokit
        uses: CompeyDev/setup-rokit@v0.1.2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set Linker
        run: |
          mv jecs.luau pesde_linker.luau
          mv wally_linker.luau jecs.luau

      - name: Wally Login
        run: wally login --token ${{ secrets.WALLY_AUTH_TOKEN }}

      - name: Wally Publish
        run: wally publish

      - name: Set Linker
        run: |
          mv jecs.luau wally_linker.luau
          mv pesde_linker.luau jecs.luau

      - name: Pesde Login
        run: pesde auth login --token "${{ secrets.PESDE_AUTH_TOKEN }}"

      - name: Pesde Publish
        run: pesde publish -y
